name: 'Build'
on:
  - workflow_dispatch
jobs:
  test:
    uses: ./.github/workflows/main.yml
    with:
      mode: '["build"]'
      mapping: '["lite"]'
      alphas: '["colemakdhk"]'
      kmonad_os: '["mac"]'
